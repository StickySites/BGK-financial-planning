import { Section } from "@/components/ui/section";

export default function ContactPage() {
  return <Section title="Contact"><p>Basic contact information page scaffold (no form) is in place.</p></Section>;
}
