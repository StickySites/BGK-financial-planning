import { Section } from "@/components/ui/section";

export default function ResourcesPage() {
  return <Section title="Resources"><p>Resources listing scaffold is ready (listing-only).</p></Section>;
}
